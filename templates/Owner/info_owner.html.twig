<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Info Owner{% endblock %}</title>
</head>
<body>
<h1>General INFO</h1>
{#TODO: meter validacion de usuario logado#}
{#{% include 'fbSdk.html.twig' %}#}
<p>Hello, {{ complete_name }}</p>
<p>This are your actual things (click or details or edition):</p>
{% for thing in things %}
    <li>
        {% if thing.connection == true %}
            <a href="{{ thing.url }}">Thing Name = {{ thing.name }} Thing Brand = {{ thing.brand }}</a>
        {% else %}
            Please check credentials or connection, found problems with thing with id {{ thing.id }} message: {{ thing.message }}.
        {% endif %}
    </li>
{% endfor %}
<h1>Add new Thing</h1>

{#
TODO not woring<form action="{{ path('thing_create') }}">
https://stackoverflow.com/questions/25728172/unable-to-generate-a-url-for-the-named-route
#}
<form action="/thing/create" method="post">
    <p>id (url)</p>
    <input type="text" name="root">
    <p>user</p>
    <input type="text" name="user">
    <p>password</p>
    <input type="password" name="password">
    <input type="submit" value="Add Thing">
</form>
</body>
</html>
